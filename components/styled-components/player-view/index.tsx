import { PlayerView } from "bitmovin-player-react-native";
import styled from "styled-components/native";

const StyledPlayerView = styled(PlayerView)`
	.bmpui-controls-shown {
		color: red !important;
	}
`;

export default StyledPlayerView;
